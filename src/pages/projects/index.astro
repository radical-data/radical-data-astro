---
import TagPage from "@layouts/TagPage.astro";
import { getCollection } from "astro:content";
const posts = await getCollection("projects");
---

<TagPage
  title="Projects | Radical Data"
  description="A collection of Radical Data projects."
  folder="/projects/"
  shortStatement="Things We've Done and Things We're Doing"
/>
